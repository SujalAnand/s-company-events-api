# API Configuration
api:
    id: "16848514"
    ver: "v1"
    application:
        name: "s-company-events-api"
    console:
        enabled: "true"
    http:
        listener:
            path: "/${api.application.name}/${api.ver}/*"
            
#Database Configuration
mssql:
    databaseName: "CompanyEvents"
    instance:
        name: "SQL_SAC_SEC_DEV"
    host: "BYMCCRSH0606"
    retry: 
        frequency: "2000"
        attempts: "3"